import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { Lock } from 'lucide-react';

export const ResetPasswordPage: React.FC = () => { const [done, setDone] = useState(false); return <div className="auth-page"><div className="auth-card"><div className="text-center"><div className="text-2xl font-extrabold text-[#0752b8]">TechPro</div><h1 className="mt-2 text-lg font-bold">Đặt lại mật khẩu</h1><p className="mt-1 text-xs text-[#667287]">Chọn mật khẩu mới, tối thiểu 6 ký tự.</p></div>{done ? <p className="mt-6 rounded bg-[#edf7f0] p-4 text-center text-sm text-[#28723c]">Mật khẩu đã được cập nhật.</p> : <form onSubmit={e => { e.preventDefault(); setDone(true); }} className="mt-6 space-y-4"><label className="form-label">Mật khẩu mới<div className="input-wrap"><Lock size={16}/><input required minLength={6} type="password" placeholder="••••••••"/></div></label><label className="form-label">Xác nhận mật khẩu<div className="input-wrap"><Lock size={16}/><input required minLength={6} type="password" placeholder="••••••••"/></div></label><button className="auth-button">Đặt lại mật khẩu</button></form>}<Link to="/login" className="mt-5 block text-center text-xs font-semibold text-[#0752b8]">Quay lại đăng nhập</Link></div></div>; };
